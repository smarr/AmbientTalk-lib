import /.at.lang.futures exclude minutes, seconds, millisec;
import /.at.support.timer;
import /.at.support.util;
enableFutures(false);

//import /.bridges.RFIDService;
import ~.RFIDService;

//def service := createAndRunRFIDservice();

def service := createRFIDService();
network.online();
service.run();
service;

self;
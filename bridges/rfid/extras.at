
// debug and info methods
def DEBUG := false;
def INFO  := true;

def debug(str) {
	if: (DEBUG) then: {
		system.println("[DEBUG] " + str);
	};
};

def info(str) {
	if: (INFO) then: {
		system.println("[INFO] " + str);
	};
};

self;